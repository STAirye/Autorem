# Paquete 'modulos' — módulos de tarea de autoREM (uno por sección/evento del REM).
# Cada uno expone un descriptor TAREA que el dispatcher (autorem.py) registra.
# Hoy: rem_a05_o_egresos, rem_a05_n_ingresos. Importan la base desde 'programas'.
# Convención: rem_<pestaña>_<casilla>_<descriptor> (casilla = celda REM: N ingresos, O egresos).
